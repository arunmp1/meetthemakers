{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "installCommand": "npm install && npm install -g node-gyp && npm rebuild bcrypt --build-from-source",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.js"
    }
  ],
  "engines": {
    "node": "18.x"
  }
}